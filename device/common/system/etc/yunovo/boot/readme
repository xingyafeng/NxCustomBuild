config文件夹：
放的是名称对照表，配置开机logo和动画时，根据‘=’号前面的名称检索后面的动画和logo文件。

logo文件夹：
放的是全屏png图片，用于开机logo配置。

media文件夹：
放的是开机动画文件。

settings文件夹：
放的是设置使用的logo图标。设置中打开开机logo设置时，加载这里面的图标。

zip_animation.py文件：
生成动画文件和配置文件的python脚本。

注：
1.不要配置客户的默认开机logo，本压缩包为公用包，默认开机logo单独配置。
2.路径不要更换，设置中会使用。
